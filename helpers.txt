git add .
git commit -m "Updated site"
git push


git add .
git commit -m "Mobile car-view fix"
git push origin main

reset demo wallet run in browsers console and refresh
localStorage.removeItem("demoWallet");

// Local demo backend
const API_BASE = "http://localhost:3001/api";

Revert local code 
git status
git fetch origin
git branch --show-current
git checkout main
git reset --hard origin/main
git clean -fd